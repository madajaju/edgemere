version: "3.7"
services:
  frontend:
    image: cpl_t_web
    networks:
      - sibling
  gw:
    image: cpl_t_gw
    networks:
      children:
        aliases:
          - parent
          - top
      sibling:
        aliases:
          - gateway

networks:
  children:
    driver: overlay
    attachable: true
    name: ${APPNAME}_cpl_t_family
  sibling:
    driver: overlay
  parent:
    external: true
    name: ${APPNAME}_cpl_family
